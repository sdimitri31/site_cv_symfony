<nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <span class="navbar-brand-title">Suso Dimitri</span>
        </a>
        <button class="navbar-toggler btn btn-navbar" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon">{{ ux_icon('clarity:menu-line') }}</span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_project_index') }}">Réalisations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact_index') }}">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_biography_index') }}">Présentation</a>
                </li>
                {% if app.user %}
                    {#                    <li class="nav-item"> #}
                    {#                        <a class="nav-link" href="{{ path('profile') }}">Profil</a> #}
                    {#                    </li> #}
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_admin') }}">Administration</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    {#<li class="nav-item">#}
                    {#    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>#}
                    {#</li>#}
                {% endif %}
            </ul>
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <button id="theme-toggle" class="btn btn-navbar">
                        <span id="icon-light" class="theme-icon active">{{ ux_icon('clarity:sun-solid') }}</span>
                        <span id="icon-dark" class="theme-icon">{{ ux_icon('clarity:moon-solid') }}</span>
                    </button>
                </li>
            </ul>
        </div>
    </div>
</nav>